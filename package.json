{
  "name": "phactoryfit",
  "version": "1.7.0",
  "private": true,
  "description": "Security metadata and repeatable checks for the PhactoryFit static PWA.",
  "scripts": {
    "test:static": "python3 tests/security_static.py",
    "audit:security": "npm run test:static && npm run test:service-worker && node --check app.js && node --check service-worker.js && npm audit --omit=dev --audit-level=high",
    "test:service-worker": "node tests/service_worker_security.js"
  },
  "dependencies": {
    "@zxing/browser": "0.2.1",
    "@zxing/library": "0.23.0",
    "@zxing/text-encoding": "0.9.0"
  },
  "engines": {
    "node": ">=24"
  }
}
