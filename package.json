{
  "name": "phactoryfit",
  "version": "1.12.0",
  "private": true,
  "description": "Security-hardened PhactoryFit PWA with Universal Food Search, a 1,348-record offline U.S. restaurant catalog, optional secure Food Cloud providers, editable timed diary, barcode logging, and cosmic UI.",
  "scripts": {
    "test:static": "python3 tests/security_static.py",
    "audit:security": "npm run test:static && npm run test:service-worker && npm run test:browser-security && npm run test:restaurant && npm run test:restaurant-data && npm run test:ui && npm run test:camera && node --check app.js && node --check restaurant-foods.js && node --check restaurant-foods-expanded.js && node --check food-cloud/worker.js && node --check service-worker.js && npm audit --omit=dev --audit-level=high",
    "test:service-worker": "node tests/service_worker_security.js",
    "test:restaurant": "python3 tests/restaurant_search.py",
    "test:browser-security": "python3 tests/browser_security.py",
    "test:ui": "python3 tests/ui_regression.py",
    "test:camera": "python3 tests/camera_regression.py",
    "test:restaurant-data": "node tests/restaurant_data_audit.js"
  },
  "dependencies": {
    "@zxing/browser": "0.2.1",
    "@zxing/library": "0.23.0",
    "@zxing/text-encoding": "0.9.0"
  },
  "engines": {
    "node": ">=24"
  }
}
